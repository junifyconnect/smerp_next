---
feature: 경영 통계
domain: 경영관리
nature: api_center
status: 진행중
progress: 55
last_reviewed: 2026-04-20
_files:
  - "app/api/management/stats/**"
---

# 경영 통계 🟡

연간 매출/매입/GP 통계, 거래처별 순위, 월말 입출금 예측 등 경영진용 리포트 API를 제공한다. 원장의 `isActive` 필터를 일관 적용해 revise 체인에서 안전하다. API는 완성되어 있고, UI 방향은 별도 리포트 페이지(`/management/reports`)로 확정되었다 — 차트 라이브러리 도입 및 리포트 레이아웃 설계가 남아 있다.

**진행률** ███████████░░░░░░░░░ 55%

## 제공 기능

- 연간 매출/매입/GP 집계 리포트 (월별 추이, 카테고리별, 담당자별)
- 매출처/매입처별 거래 순위 + 점유율
- 월말 기준 외상매출금(receivables)/외상매입금(payables) 예측 + 순현금흐름

## 완료

- ✅ 연간 리포트 API — 총매출/총매입/GP, 월별 추이(12개월), 카테고리별 집계, 담당자별 매출
- ✅ 회사별 매출/매입 순위 API — 기간 파라미터, 순위별 점유율(sharePercent) 계산
- ✅ 월별 입출금 예측 API — 결제예정일 기준 receivables/payables, 업체별 그룹핑, netCashFlow
- ✅ 모든 집계 쿼리(총매출/총매입, 월별 추이 12회, 카테고리별, 담당자별, 회사 순위, 월별 예측)에 `isActive: true` 필터 일관 적용 — revise 체인 정합성 보장
- ✅ GP 계산 fallback (grossProfit 필드 없으면 매출-매입 차액으로 대체)
- ✅ UI 방향 확정 (별도 리포트 페이지 `/management/reports`. 대시보드에는 통합하지 않음)

## 남은 것

- ❌ `app/(main)/management/reports/` 페이지 생성 (경영 리포트 라우트)
- ❌ 차트 라이브러리 선정 및 도입 (recharts 또는 Chart.js)
- ❌ 연간 매출/매입/GP 리포트 뷰 (월별 추이 라인차트, 카테고리별 도넛, 담당자별 바차트)
- ❌ 회사별 순위 테이블/차트 (top N, sharePercent 시각화)
- ❌ 월별 예측 차트 (receivables/payables/netCashFlow)
- ❌ 사이드바 메뉴에 "경영 리포트" 추가
- ❌ 기간 비교(전년 동기 대비 등) 분석

## API 엔드포인트

- `GET /api/management/stats/annual-report?year=YYYY` — 연간 매출/매입/GP + 월별·카테고리별·담당자별 집계
- `GET /api/management/stats/company-rank?type=sales|purchase&startDate&endDate&limit` — 거래처 순위 + 점유율
- `GET /api/management/stats/monthly-forecast?year&month` — 해당 월 외상매출/매입 예측 + 순현금흐름

---

<details>
<summary>🔧 기술 정보 (개발자용)</summary>

**Entry points**
- `app/api/management/stats/annual-report/route.ts` — 연간 매출/매입/GP + 월별(12회 aggregate) + 카테고리·담당자 groupBy
- `app/api/management/stats/company-rank/route.ts` — 매출/매입처 순위 (type 파라미터로 분기)
- `app/api/management/stats/monthly-forecast/route.ts` — 월말 입출금 예정 (paymentDueDate 기준)

**주요 라이브러리**: 없음 (차트 라이브러리 도입 예정)

**의존성 서비스**: PostgreSQL (Prisma — SalesLedger, PurchaseLedger 집계). 모든 쿼리 `isActive: true` 필수.

</details>
