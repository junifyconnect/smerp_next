---
title: 스모크 체크 (브라우저 E2E)
tested_at: 2026-04-23
tester: Claude + preview MCP
account: superadmin / superadmin
env: Next 16.1.1 dev (Turbopack), port 3004
---

# 스모크 체크 (브라우저 E2E)

## 2026-04-23 13:55 — 브랜치 feature/e2e-auto-full-coverage — 플로우: all + 미검증 분기 (🤖 자동 수정 모드)

**총평**: ✅ Pass 1~9 전체 통과 · 🤖 자동 수정 **0건** · ⏸️ 보류 **2건** (MA 알림 미구현 / 반려 후 재상신 플로우)

### Pass 1 — 페이지 렌더 (26/26 ✅)
모든 라우트 HTTP 200, 콘솔 에러 없음.

### Pass 2 — 영업 기본 플로우 ✅
- SA-2026-0007 create → submit(결재자 3명) → TEAM_LEADER → CEO → APPROVED
- 매출/매입 InvoiceRecord + SalesLedger/PurchaseLedger 자동 생성

### Pass 3 — MA 기본 플로우 ✅
- MA-2026-0010 create (billingDayOfMonth=25) → submit → 3단계 → APPROVED
- MABilling 2건 생성 (dueDate: 5/25, 6/25 정확)

### Pass 4 — 영업 반려 ✅ (발견: 재상신 플로우 이슈)
- SA reject: PENDING_TEAM_LEAD → REJECTED + rejectionReason + rejectedBy 기록
- ⚠️ REJECTED 상태에서 submit 재호출 시 400 (아래 보류 2번 참조)

### Pass 5 — 영업 회수 ✅
- SA withdraw: PENDING_TEAM_LEAD → DRAFT + 결재자 ID 초기화
- 회수 후 재상신: DRAFT → submit → PENDING_TEAM_LEAD **정상 재시작**

### Pass 6 — MA 반려/회수 ✅
- MA reject: PENDING_TEAM_LEAD → REJECTED + 사유
- MA withdraw: PENDING_TEAM_LEAD → DRAFT

### Pass 7 — 견적서 → 품의서 (sourceQuoteId) ✅
- 견적서 생성 → `quoteId` 전달로 품의서 생성
- sourceQuoteId 정상 연결, 매출처/제품/품목 자동 복제 (SA-2026-0010)

### Pass 8 — 품의서 revise + compare API ✅
- v1 APPROVED 후 revise → v2 (SA-2026-0011, version=2, originalId=v1) 생성
- `/api/sales-approvals/:id/compare?baseId=v1Id` → versions/currentVersion/previousVersion/comparison 필드 정상 반환

### Pass 9 — 대시보드 3역할 뷰 ✅
- ADMIN (CEO 뷰) / FINANCE (경영팀 뷰) / SALES (영업팀 뷰) 모두 /dashboard 200 OK
- 각 역할별 세션에서 페이지 렌더 확인 완료

### Pass 10 — MA 알림 발송 ⏸️ 보류
- **발견**: MA 결재 API 4종(submit/sign/reject/withdraw) 모두 **알림 발송 호출 없음**
- DB 확인: notifications 테이블에 `relatedType=SalesApproval` 12건, `relatedType=MAApproval` **0건**
- 영업은 `lib/notifications/sender.ts`의 함수들 호출 중, MA는 해당 sender 함수 미구현

### 이슈 요약

#### ⏸️ 보류 1 — MA 품의서 알림 미구현
- `lib/notifications/sender.ts`에 `notifyMAApprovalSubmitted/TeamLeadSigned/Approved/Rejected/Withdrawn` 5종 추가 필요
- MA submit/sign/reject/withdraw에서 호출 추가
- 영업 알림 함수 패턴 그대로 복사 + linkUrl만 `/ma/approvals/...`로 교체
- **자동 수정 scope 제외**: 5개 파일 수정 + 신규 함수 ~60줄, feat 작업 규모
- **권장**: 별도 PR `feat/ma-approval-notifications`

#### ⏸️ 보류 2 — 반려된 영업 품의서 재상신 불가
- 증상: REJECTED 상태에서 submit 재호출 → 400 "작성중 상태의 품의서만 기안할 수 있습니다"
- 실무상 "반려받은 후 수정 → 재상신" 경로 불명확
- 해결 선택지 (기획 판단 필요):
  - A: REJECTED → 작성자가 편집 시 DRAFT 자동 전환 + rejectionReason은 history로 남김
  - B: REJECTED에서 바로 submit 허용 (reason 초기화)
  - C: "재상신" 전용 버튼/API 추가
- **자동 수정 scope 제외**: 플로우 설계 결정 영역 (API 시그니처/권한 로직)

### 자동 수정 결과
- 🤖 수정 **0건** (발견 이슈 2건 모두 보류 scope)
- ⏸️ 보류 **2건**
- typecheck/lint 클린, working tree 변경 없음

### 검증 매트릭스

| 구분 | 영업 | MA |
|---|---|---|
| 기본 플로우 (create → sign → approve) | ✅ | ✅ |
| 반려 | ✅ | ✅ |
| 회수 (+ 재상신) | ✅ | — |
| 견적서 → 품의서 | ✅ | — |
| revise + compare | ✅ | — |
| 대시보드 역할별 뷰 | ✅ (3역할) | ✅ |
| **알림 발송** | ✅ | ❌ (보류 1) |
| **반려 후 재상신** | ❌ (보류 2) | — |

---

## 2026-04-22 17:00 — 브랜치 feat/ma-billing-cycle-ui — 플로우: all (🤖 자동 수정 모드)

**총평**: ✅ 전체 통과 (26/26 페이지 + 모든 핵심 플로우) · 🤖 자동 수정 0건 · ⏸️ 보류 0건

### Pass 1 — pages (26/26 ✅)
모든 페이지 HTTP 200. 영업/MA/경영관리/관리자/공통 도메인 전체.

### Pass 2 — 영업 전체 플로우 ✅
- SA-2026-0003 DRAFT 생성
- **submit(결재자 3명) → PENDING_TEAM_LEAD** (BUSINESS_RULES §5 반영 확인)
- **sign role=SALES_MANAGER 거부 (400)** — "SALES_MANAGER는 submit API에서 처리"
- TEAM_LEADER → PENDING_CEO → CEO → APPROVED
- 매출/매입 InvoiceRecord 자동 생성 (200k / 140k PENDING)

### Pass 3 — MA 전체 플로우 ✅
- MA-2026-0007 생성 (billingDayOfMonth=20 지정)
- 3단계 결재 APPROVED
- MABilling 3건 자동 생성:
  - 5/20, 6/20, 7/20 (청구일 20일 정확 반영)
  - 매출 매월 20만, 매입 분기(5월만) 10만 — 주기 로직 정상

### Pass 4 — 계산서 상태 전이 ✅
- MABilling → InvoiceRecord 파생 발행 (source=MA_BILLING)
- **중복 발행 방어 409**: "이미 발행된 매출 계산서가 있습니다"
- **빈 body 방어 400**: "id 또는 maBillingId가 필요합니다"
- AMEND: source/maBillingId 복제, 원본 CANCELLED + 신규 PENDING
- CANCEL + 중복 CANCEL 409 방어
- month 파라미터 검증 400 (YYYY-MM 형식)

### Pass 5 — 집계 API UNION ✅
- `sales-ledger`: 영업 3 + MA 18 = 21건 통합
- `purchase-ledger`: 영업 3 + MA 6 = 9건
- `annual-report`: breakdown 분리 (salesApproval 300k + maBilling 1,890k = 총 2,190k)
- `invoice-status`: groups 3개(영업) + maGroups 5개(MA)

### Pass 6 — revise + 반려/회수 ✅
- 영업 revise: SA-2026-0003 → SA-2026-0004 (v2, isLatest=true)
- 영업 reject: PENDING_TEAM_LEAD → REJECTED + 사유 기록
- 영업 withdraw: PENDING_TEAM_LEAD → DRAFT
- MA reject: PENDING_TEAM_LEAD → REJECTED
- MA withdraw: PENDING_TEAM_LEAD → DRAFT

### 자동 수정 결과
- 🤖 수정 **0건** — 회귀 없음, 모든 규칙 정상 작동
- ⏸️ 보류 **0건**
- typecheck/lint 클린, working tree 변경 없음

### 지난 실행 대비 개선점
- MA 품의서 핵심 플로우 전부 정상 (PR #9~#15 통합 결과)
- 결재선 명시 지정 (BUSINESS_RULES §5 반영)
- MABilling의 dueDate가 billingDayOfMonth 정확히 반영
- 영업+MA UNION 집계 완성

---

## 2026-04-21 19:04 — 커밋 2469575 — 플로우: all (🤖 자동 수정 모드)

**총평**: ✅ 전체 통과 (26/26 페이지 + 14/14 단계) · 🤖 자동 수정 1건 · ⏸️ 보류 1건

### Pass 1 — pages (26/26 ✅)
- 영업 6/6, MA 4/4, 경영관리 UI+API 4/4, 관리자 CRUD 6/6, 관리자 데모 3/3, 공통 3/3
- 모든 라우트 HTTP 200

### Pass 2 — masters
- ✅ `(주)테스트컴퍼니-E2E-20260421` 등록 (customer id 68c05c7f)
- ✅ `(주)테스트공급사-E2E-20260421` 등록 (vendor id 7e37dc11)
- ✅ 팀장 `김팀장-E2E-20260421` (user id a644938e, role=TEAM_LEADER, dept=SALES) 등록
- ✅ superadmin / 팀장 서명 이미지 S3 업로드 (presigned URL 반환 확인)

### Pass 3 — approval
- ✅ 품의서 `SA-2026-0001` 생성 (code=S260421-01, 매출 200,000 / 매입 140,000)
- ✅ 3단계 결재 DRAFT → PENDING_TEAM_LEAD → PENDING_CEO → APPROVED

### Pass 4 — invoice
- ✅ 승인 시 자동 생성: SalesLedger 1건(VAT 20,000), PurchaseLedger 1건(VAT 14,000), InvoiceRecord 매출/매입 각 1건 (PENDING)
- ✅ ISSUED 전이: INV-2026-0001 / PINV-2026-0001
- ✅ AMEND (매출 100,000 → 110,000): 원본 CANCELLED + 신규 PENDING 220,000 체인 (amendedFromId 연결)
- ✅ CANCEL (매입 `착오 발행`) + 중복 CANCEL 409 "이미 취소된 계산서입니다" 방어
- ℹ️ cancel API 파라미터명 `reason` (문서 기록용)

### Pass 5 — revise
- ✅ revise → `SA-2026-0002` v2 생성, v1 `isLatest=false` 전환
- ✅ v2 3단계 결재 후 APPROVED
- ✅ v2 승인 시 v1 원장 `isActive=false`(REVISED_v2), v2 원장 신규 생성 확인
- ✅ v1 InvoiceRecord 체인 보존 (PENDING→CANCELLED, ISSUED→NEEDS_AMENDMENT, AMEND PENDING→CANCELLED)

### 이슈
- 🤖 **[자동 수정]** `/api/management/invoice-status` `month` 파라미터 검증 부재
  - 증상: `?month=abc`, `?month=2026` 등 YYYY-MM이 아닌 값 → `new Date(NaN)` → Prisma에 그대로 전달 → 500
  - 수정: `app/api/management/invoice-status/route.ts:122-141` — 정규식 `^(\d{4})-(\d{2})$` 검증 + 월 1~12 범위 체크, 위반 시 400 반환
  - 검증: 유효한 `2026-04` 200, `abc`/`2026` 400 (YYYY-MM 형식 오류), `2026-13`/`2026-00` 400 (월 범위 오류)
  - typecheck/lint 통과

- ⏸️ **[보류]** 거래처 API 필드명 불일치 (Customer `companyName` vs Vendor `name`)
  - 스키마·API 시그니처 변경 범위라 자동 수정 제외. 사용자 판단 필요.

### 자동 수정 파일 목록
- `app/api/management/invoice-status/route.ts`

---

## 2026-04-21 (이후) — 🧹 E2E 데이터 정리 (scope=all)

- 품의서 1건(SA-2026-0001), InvoiceRecord 3건, 매출장/매입장 각 1건
- 매출처 1개, 매입처 1개, 사용자 1명(teamlead1/김팀장) 삭제
- superadmin 계정 유지 · 서명 이미지 S3 객체는 별도 정리 필요 (스크립트 미포함)
- 기준점 리셋 · 다음 `/project-spec:e2e-auto all` 실행 시 clean slate에서 시작

---

> ⚠️ **[HISTORICAL — PR #3/#4 이전 상태]**
> 아래 2026-04-21 섹션은 **UI 간소화(PR #3)**·**스키마 정리(PR #4)** 이전 실행 결과입니다.
> 당시 기록된 "카테고리 5종 / 세금유형 TAX·ZERO·EXEMPT / 매출 단위 PRODUCT·ITEM"은 현재 설계와 다릅니다.
> 최신 기준은 `docs/features/BUSINESS_RULES.md`를 참고하세요. 회귀가 아니라 **과거 스냅샷**입니다.

---

**수행일**: 2026-04-21
**범위**: Pass 1 페이지 렌더 + Pass 2 등록·결재·계산서 전체 플로우 (E2E)

범례: ✅ 작동 · 🟡 렌더 OK / 기능 일부 · ⚠️ 이슈 · ❌ 실패

---

## Pass 1 — 페이지 렌더 (24개 라우트)

모든 페이지 HTTP 200, 콘솔 에러 없음.

| 도메인 | 페이지 | 상태 |
|-------|-------|------|
| 영업 | /sales/quotes, /new, /approvals, /new, /orders, /new | ✅ 6/6 |
| MA | /ma/quotes, /ma/approvals (+ /new) | ✅ 4/4 |
| 경영관리 | /management/invoice-status | ✅ |
| 경영관리 API | /api/management/{sales-ledger,purchase-ledger,stats/annual-report} | ✅ 200 (UI 없음, api_center 의도대로) |
| 관리자 | /admin/{customers,vendors,users,leaves,excel-templates,import} | ✅ 6/6 |
| 관리자 데모 | /admin/{system-architecture,process-demo,project-intro} | ✅ 3/3 |
| 공통 | /dashboard, /calendar, /mypage | ✅ 3/3 (대시보드는 하드코딩 샘플) |
| 시스템 | /api/auth, /api/notifications, /api/cloud | ✅ 200 |

**재설계 UI 반영**: 영업 품의서 신규 폼에 카테고리(상품/MA/건물임대/장비임대/일반경비), 세금(과세/영세/면세), 매출계산서 단위(제품/품목), 매입계산서 매입처별 자동그룹 UI 모두 노출됨.

---

## Pass 2 — E2E 전체 플로우 실행 결과

직접 등록·결재·계산서 전이까지 실행한 결과. **핵심 재설계 로직(InvoiceRecord 단일 진실 소스) 전부 작동 확인.**

### 1. 마스터 데이터 등록

| # | 작업 | API | 결과 |
|---|------|-----|------|
| 1 | 매출처 "(주)테스트컴퍼니" 등록 | POST /api/customers (`companyName` 필드) | ✅ id 발급, isActive=true |
| 2 | 매입처 "(주)테스트공급사" 등록 | POST /api/vendors (`name` 필드) | ✅ id 발급 |
| 3 | superadmin 서명 업로드 | POST /api/users/:id/signature (FormData + PNG) | ✅ S3 실제 업로드 + presigned URL 반환 |
| 4 | 팀장 "김팀장" 등록 (SALES 팀/팀장) | POST /api/users | ✅ id 발급 |
| 5 | 팀장 서명 업로드 | POST /api/users/:id/signature | ✅ S3 업로드 |

⚠️ **API 스키마 불일치 발견**: Customer는 `companyName` 필드, Vendor는 `name` 필드. 같은 "거래처" 계열인데 필드명이 다름 — 통일하거나 문서화 필요.

### 2. 품의서 생성 + 결재 3단계

| # | 단계 | API | 결과 |
|---|------|-----|------|
| 6 | 품의서 생성 (카테고리=상품, 세금=TAX, 단위=PRODUCT) | POST /api/sales-approvals | ✅ approvalNumber=`SA-2026-0001`, status=DRAFT, 매출 200,000 / 매입 140,000 |
| 7 | 영업담당 서명 (DRAFT → PENDING_TEAM_LEAD) | POST /api/sales-approvals/:id/sign role=SALES_MANAGER | ✅ |
| 8 | 팀장 서명 (PENDING_TEAM_LEAD → PENDING_CEO) | role=TEAM_LEADER | ✅ |
| 9 | CEO 서명 (PENDING_CEO → APPROVED) | role=CEO | ✅ approvalDate 자동 세팅 |

✅ **3단계 결재 파이프라인 정상 작동**. 서명 이미지(`signatureUrl`) 미등록 시 서명 거부(400) 방어도 확인.

### 3. 승인 시 자동 생성 (핵심 재설계 검증)

품의서 APPROVED 시점에 트랜잭션으로 일괄 생성됨:

| 자동 생성물 | 결과 | VAT 계산 |
|-----------|------|---------|
| **매출장 (SalesLedger)** | ✅ 1건 · 제품 단위 · 공급 200,000 · VAT 20,000 · 합계 220,000 | TAX 10% 정확 |
| **매입장 (PurchaseLedger)** | ✅ 1건 · 품목 단위 · 공급 140,000 · VAT 14,000 · 합계 154,000 | TAX 10% 정확 |
| **InvoiceRecord (매출)** | ✅ PENDING · 제품 단위(`salesInvoiceUnit=PRODUCT`) · 1건 | 200,000 |
| **InvoiceRecord (매입)** | ✅ PENDING · 매입처별 자동 그룹 · 1건 | 140,000 (`vendorCompany=(주)테스트공급사`) |

모두 `sourceProductId` / `sourceItemId` / `approvalVersion=1` / `isActive=true`로 원본 품의서 행에 역추적 가능한 상태로 연결됨. **재설계 의도대로 InvoiceRecord가 단일 진실 소스로 동작.**

### 4. 계산서 상태 전이 (ISSUED → AMEND → CANCEL)

| # | 전이 | API | 결과 |
|---|------|-----|------|
| 10 | 매출 계산서 발행 (PENDING → ISSUED) | POST /api/management/invoices/issue + `invoiceNumber=INV-2026-0001` | ✅ status=ISSUED, invoiceDate 자동 세팅 |
| 11 | 매입 계산서 발행 | PINV-2026-0001 | ✅ ISSUED |
| 12 | 매출 수정발행 AMEND (단가 100,000 → 110,000) | POST /api/management/invoices/amend | ✅ 원본 CANCELLED(reason="단가 정정") + 신규 PENDING 레코드(`amendedFromId=원본`, 단가 110,000, 합계 220,000) |
| 13 | 매입 계산서 취소발행 CANCEL | POST /api/management/invoices/cancel | ✅ status=CANCELLED, cancelReason="착오 발행" |
| 14 | 중복 CANCEL 시도 | 같은 API 재호출 | ✅ 409 "이미 취소된 계산서입니다" 방어 |

### 5. 계산서 발행현황 화면 확인

`/management/invoice-status` 페이지에서 위 모든 상태가 UI로 정확히 반영됨:
- 매출 합계 220,000원 1건 (amend 후 신규 PENDING 레코드)
- 매입 합계 0원 (취소되어 "취소 숨기기" 필터로 숨겨짐)
- "수정본" 마커로 amend 흔적 시각적 표시
- 상태별 집계 패널에 "매출 상태별: 미발행 1건" 표시

---

## 총평

- **모든 핵심 플로우 작동**: 마스터 등록 → 품의서 생성 → 3단계 결재 → 원장·InvoiceRecord 자동 생성 → 계산서 발행 → 수정발행 → 취소발행까지 전체 파이프라인 통과.
- **재설계 정합성 확인**: InvoiceRecord가 단일 진실 소스로 매출/매입 모두 품의서 승인 시 자동 생성되고, AMEND 시 원본 CANCELLED + 신규 PENDING 체인 연결 정상.
- **S3 실제 업로드 작동**: 서명 이미지가 실제 AWS S3 버킷에 올라가고 presigned URL 반환됨.
- **VAT 계산 정확**: TAX 10% / ZERO / EXEMPT 분기 설계대로 동작.

## 발견된 이슈

1. ⚠️ **API 필드명 불일치**: Customer는 `companyName`, Vendor는 `name`. 거래처 계열인데 스키마 통일 안 됨.
2. ⚠️ **invoice-status 500 에러 (파라미터 잘못 넘긴 경우)**: `?year=&month=` 형태로 호출하면 `new Date("Invalid Date")` 생성 후 Prisma에 그대로 전달되어 500. 파라미터 검증 추가 권장.
3. 🟡 **대시보드 하드코딩**: 샘플 데이터(156건/245,000,000원 등)가 그대로 표시됨 — spec에 미착수 10%로 명시된 대로.

## 미검증 (필요 시 다음 스모크에서)

- 품의서 REVISE 체인 (v2 생성 시 이전 원장 `isActive=false` + ISSUED 계산서 → NEEDS_AMENDMENT 전이)
- 견적서 기반 품의서 생성 (sourceQuoteId 연결)
- 엑셀 업로드 실제 파일 파싱
- MA 품의서 플로우
- 반려(reject) / 회수(withdraw) 액션
- 알림 벨 UI
