/**
 * @deprecated 2026-04-20 이후 사용하지 않음.
 *
 * 재설계 이전 InvoiceStatusFilters(구 vendorName 필드 포함) 기반 필터 컴포넌트.
 * 새 필터 UI는 ../page.tsx 상단 필터바에 인라인으로 통합되었다.
 */
export default function InvoiceFilterBar_Deprecated() {
  return null
}
