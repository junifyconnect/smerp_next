/**
 * @deprecated 2026-04-20 이후 사용하지 않음.
 *
 * 재설계(InvoiceRecord 행 단위) 이전의 FlatInvoiceRow 기반 컴포넌트.
 * 새 UI는 ../page.tsx 내부 GroupRows 컴포넌트가 렌더링을 담당한다.
 *
 * 삭제 전까지 빌드 에러를 내지 않기 위한 no-op export.
 */
export default function InvoiceStatusTable_Deprecated() {
  return null
}
