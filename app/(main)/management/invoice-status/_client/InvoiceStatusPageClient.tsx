/**
 * @deprecated 2026-04-20 이후 사용하지 않음.
 *
 * 재설계 이전 flattenGroups + FlatInvoiceRow 기반 클라이언트 컴포넌트.
 * 새 구현은 ../page.tsx 자체가 `"use client"` + 전용 상태/렌더링을 담당한다.
 */
export default function InvoiceStatusPageClient_Deprecated() {
  return null
}
